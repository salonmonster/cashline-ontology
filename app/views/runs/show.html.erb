<% content_for(:title, "Run #{@run.directory_token}") %>
<nav class="text-sm text-slate-600 mb-3"><%= link_to "Runs", runs_path, class: "underline" %> / <span class="font-mono"><%= @run.directory_token %></span></nav>

<%= turbo_stream_from [@run, Current.user] if Current.user %>
<%= render "panel", run: @run %>
